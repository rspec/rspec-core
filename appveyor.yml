# This file was generated on 2020-09-27T11:30:20+02:00 from the rspec-dev repo.
# DO NOT modify it by hand as your changes will get lost the next time it is generated.

version: "{build}"

# This will build all PRs targetting matching branches.
# Without this, each PR builds twice -- once for the PR branch HEAD,
# and once for the merge commit that github creates for each mergable PR.
branches:
  only:
    - main

# Disable normal Windows builds in favor of our test script.
build: off

cache:
  - vendor/bundle

install:
  - set PATH=C:\Ruby%RUBY_VERSION%\bin;%PATH%
  - bundle config --local path vendor/bundle
  - gem install childprocess -v '1.0.0' --verbose --backtrace --debug
  - cat C:/projects/rspec-core/vendor/bundle/ruby/2.2.0/extensions/x86-mingw32/2.2.0/childprocess-1.0.0/gem_make.out
  - bundle install
  - cinst ansicon

before_test:
  - ruby --version
  - gem --version
  - bundle --version

test_script:
  - bundle exec rspec --backtrace

environment:
  matrix:
    - ruby_version: 21
    - ruby_version: 22
